"""Tiny production module for SUT / coverage linkage fixtures."""


def add(a: int, b: int) -> int:
    return a + b


def multiply(a: int, b: int) -> int:
    return a * b


def incidental(x: int) -> int:
    return x + 1
